<!DOCTYPE html>
<html>
	<head>
		<meta charset = "utf-8" />
	</head>
	<style>
		*{
			border-box:box-sizing;
			margin: 0;
			padding: 0;
		}

		nav{
			display:flex;
			flex-direction:row;
		}

		nav > div{
			padding-right:10px;
		}

		.flex-row{
			display:flex;
			flex-direction:row;
			margin-top:30px;
		}

		.container-book{
			border:1px solid black;
			margin:0 10px 0 0;
		}
	</style>
	<body>
		<nav>
			<div><a href = "/home">Home</a></div>
			<div><a href = "/book-list">Book List</a></div>
			<div><a href = "/about">About</a></div>
			<div><a href = "/login">Login</a></div>
		</nav>
		<div class = "flex-row" >
			<div class = "container-book">
					<img src = "<%= result[0].path_img %>"  width = "100" height = "100"/><br />
					Title : <%= result[0].title %><br />
					Author: <%= result[0].author %><br />
					Year  : <%= result[0].year %><br />
					Pages  : <%= result[0].pages %><br />
					Status: <%= result[0].rent_status %><br />
				<br />
				<% if(result[0].rent_status === false){%>
					<a href = "/book-rent/<%= result[0].code %>"><button>Rent This Book</button></a>
				<% } else{%>
					<button disabled>Rent This Book</button>
				<% } %>
			</div>
		</div>
	</body>
</html>
